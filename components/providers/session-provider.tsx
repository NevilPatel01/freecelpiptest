"use client"

/** Appwrite auth context — named `SessionProvider` for stable imports after migrating off NextAuth. */
export { AppwriteAuthProvider as SessionProvider } from "@/components/providers/appwrite-auth-provider"
